// @ts-nocheck
import express from 'express';

export function createCapabilitiesRouter(_ctx): import("express").Router {
  const router = express.Router();
  // Phase 4: GET /api/v2/capabilities
  return router;
}
